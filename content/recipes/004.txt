Slug: bob
Title: Bob
Description: Swing과 달리 회전이 아니라 통통 튀듯 반복되는 상하 움직임.
Property: Layer → Transform → Position

Use Cases:
- 🔴 버튼 강조
- 🖱️ 클릭 유도 아이콘
- ⬇️ 스크롤 유도 화살표

Expression:
```jsx
freq = 2; amp = 12;
value + [0, Math.abs(Math.sin(time * freq)) * -amp];
```

Controls:
- freq | 튀는 속도 | 1 ~ 3
- amp | 튀는 높이(px) | 5 ~ 20

Result:
한쪽 방향(위)으로만 통통 튀는 리드미컬한 움직임.

Cautions:
- `Math.abs()`로 감싸져 있어 항상 위로만 튑니다. 아래로도 오가게 하려면 `Math.abs()`를 빼고 `Math.sin(time*freq)*amp`만 쓰면 #01 Float과 비슷해집니다.

Tips:
- 클릭/스크롤 유도 아이콘에 쓸 땐 amp를 너무 크게 하지 마세요(10px 이하 권장) — 과하면 산만해 보입니다.

Related:
- 012
